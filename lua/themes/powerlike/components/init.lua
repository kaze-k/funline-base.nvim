local left = require("themes.powerlike.components.left")
local right = require("themes.powerlike.components.right")
local separator = require("themes.powerlike.components.separator")

local M = {
  left = left,
  right = right,
  separator = separator,
}

return M
