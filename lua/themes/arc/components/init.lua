local left = require("themes.arc.components.left")
local mid = require("themes.arc.components.mid")
local right = require("themes.arc.components.right")
local separator = require("themes.arc.components.separator")
local special = require("themes.arc.components.special")

local M = {
  left = left,
  mid = mid,
  right = right,
  separator = separator,
  special = special,
}

return M
